export function applyViewState(nextView) {
  // This would be implemented with the appRef context
  // For now, we'll export a function that can be used in the context
}

export function goHome(nextView) {
  // This would be implemented with the appRef context
}